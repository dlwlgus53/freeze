<a href="/data"><button>관리페이지로</button></a>
<h1>Time</h1>

    <div>
        <form action="/data/time/addtime" method="POST">
            <span>시간 <input type="text" name="name" placeholder="시간의 이름을 입력하세요"/></span>
            <span> 위치 <input type="text" name="comment" placeholder="위치를 입력하세요"/></span>
            <input type="submit" value="추가하기"/>
        </form>
    </div> 
<h2>목록</h2>

<table>
        <tr>
            <th>번호</th>
            <th>이름</th>
            <th>위치</th>
            <th>수정</th>
        </tr>
        
        
        <% var i = 1; %>
        <% data.forEach(function(data) { %>
            
            <tr>
                    <td><%= i %></td>
                    <td><%= data.timename %></td>
                    <td><%= data.timecomment %></td>
                    <% link = "/data/time/deletetime?timeid=" + data.timeid %>
                    <form action=<%= link %> method="POST" >
                        <td><input type="submit" value="삭제"/></td>
                    </form>
                    <% i=i+1; %>
            </tr>
            
            <% }) %>
                
           
   
</table>

</div>